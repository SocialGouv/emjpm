---
kind: Service
apiVersion: v1
metadata:
  name: emjpm-api${HASH_BRANCH_NAME}
  labels:
    app: emjpm-api${HASH_BRANCH_NAME}
    branch: emjpm${HASH_BRANCH_NAME}
spec:
  selector:
    app: emjpm-api${HASH_BRANCH_NAME}
  ports:
  - port: ${PORT}
    name: api
