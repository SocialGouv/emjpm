1. Get the webhook token by running these commands:
kubectl -n {{ .Release.Namespace }} get secret {{ .Release.Name }} -ojsonpath='{.data.token}' | base64 --decode
2. Call your webhook using the url:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ . }}/hooks/status
  {{- end }}
{{- end }}
{{- end }}