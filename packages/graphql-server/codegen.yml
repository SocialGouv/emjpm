overwrite: true
schema: "http://localhost:4001/graphql"
documents: null
generates:
  src/types/resolvers-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"