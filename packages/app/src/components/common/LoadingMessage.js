const LoadingMessage = () => (
  <div style={{ display: "flex", alignItems: "center" }}>
    <div
      style={{
        margin: "25px 0",
        flex: "1 0 auto",
        textAlign: "center"
      }}
    >
      Chargement en cours...
    </div>
  </div>
);

export default LoadingMessage;
