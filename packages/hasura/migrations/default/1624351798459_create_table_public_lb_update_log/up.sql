CREATE TABLE "public"."lb_update_log" ("id" serial NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "user_id" integer NOT NULL, "service_id" integer, "lb_user_id" integer, "op" text, "comment" text, PRIMARY KEY ("id") , FOREIGN KEY ("service_id") REFERENCES "public"."services"("id") ON UPDATE set null ON DELETE set null, FOREIGN KEY ("lb_user_id") REFERENCES "public"."lb_users"("id") ON UPDATE set null ON DELETE set null);
