CREATE TABLE "public"."dispo_departements" ("id" serial NOT NULL, "departement_code" text NOT NULL, "dispo" integer, "mandataire_id" integer, "service_id" integer, PRIMARY KEY ("id") , FOREIGN KEY ("departement_code") REFERENCES "public"."departements"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("mandataire_id", "departement_code"), UNIQUE ("service_id", "departement_code"));
